class CalendarEntry {
    constructor({ id, userId, eventId, datetime}) {
      this.id = id;
      this.userId = userId;
      this.eventId = eventId;
      this.datetime = datetime;
    }
  }
  
export default CalendarEntry;
  