class Profile {
  constructor({ id, name, email, goals, stats, interests }) {
    this.id = id;
    this.name = name;
    this.email = email;
    this.goals = goals;
    this.stats = stats;
    this.interests = interests;
  }
}

export default Profile;
